## (1) download datasets and put them in datasets/
## (2) preprocessing (runs on all data)
## (3a) ground-truth rationale generation (runs on all data, results in labels.csv)
## (3b) eval (runs on test only, results in eval-<name>.csv)
## (4) judge (runs on test only, results in judge-<name>.csv)
