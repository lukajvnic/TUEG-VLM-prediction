PyYAML
pydantic
langchain-core
langchain-ollama
mne
matplotlib
numpy
openai
Pillow
# fine-tune track (train/, eval/score.py): torch + transformers stack, same venv
torch
torchvision   # transformers 5 image processors import it; without it every VLM load dies with a bare ImportError (2026-09-23)
transformers  # unpinned: the wheelhouse gave 5.8.0 on 2026-09-23 (the 24 first fine-tunes) and 5.14.1 on 09-26; helpers/preflight.py checks whatever is installed
peft
accelerate
bitsandbytes
huggingface_hub
lm-format-enforcer
sentencepiece # repos that ship only tokenizer.model (llava-v1.6-34b): without it transformers 5 tries tiktoken and fails
protobuf
tiktoken
